<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        tools:background="@drawable/game_bg"
        tools:ignore="SpUsage">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnPlayRegular"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:background="#fff"
            android:text="@string/play_random_game"
            android:textAllCaps="false"
            android:textColor="#000"
            android:textSize="20dp"
            android:textStyle="bold"
            tools:text="Rastgele Oyun" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnPlayAgainstTime"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="#fff"
            android:text="@string/play_against_time"
            android:textAllCaps="false"
            android:textColor="#000"
            android:textSize="20dp"
            android:textStyle="bold"
            tools:text="Zamanlı Oyun" />
    </LinearLayout>
</layout>